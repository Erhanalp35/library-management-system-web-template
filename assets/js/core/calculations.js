export const today=()=>new Date().toISOString().slice(0,10);export function loanStatus(l){if(l.status==='Lost')return'Lost';if(l.returnedDate)return'Returned';const days=Math.ceil((new Date(l.dueDate+'T23:59:59')-new Date())/86400000);return days<0?'Overdue':days<=3?'Due Soon':'Active'}export const overdueDays=(due,returned=today())=>Math.max(0,Math.ceil((new Date(returned+'T12:00:00')-new Date(due+'T12:00:00'))/86400000));export const fineAmount=(due,returned,rate)=>Math.max(0,+(overdueDays(due,returned)*Number(rate)).toFixed(2));export function bookStatus(b){if(b.status==='Archived')return'Archived';return b.availableCopies<=0?'Unavailable':b.availableCopies<=1?'Limited Availability':'Available'}export function reconcileBook(b){for(const k of['availableCopies','borrowedCopies','reservedCopies','lostCopies','damagedCopies'])b[k]=Math.max(0,Number(b[k])||0);b.totalCopies=b.availableCopies+b.borrowedCopies+b.reservedCopies+b.lostCopies+b.damagedCopies;b.status=bookStatus(b);return b}export const money=(n,c='USD')=>new Intl.NumberFormat('en-US',{style:'currency',currency:c}).format(Number(n)||0);export const dateFmt=(d)=>d?new Intl.DateTimeFormat('en-US',{dateStyle:'medium'}).format(new Date(d+'T12:00:00')):'—';
